import { SecCustomDirective } from './sec-custom.directive';

describe('SecCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new SecCustomDirective();
    expect(directive).toBeTruthy();
  });
});
